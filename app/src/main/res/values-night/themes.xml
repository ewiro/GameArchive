<resources>
    <!-- MIUI X 深色主题 (Compose + miuix-ui 项目) -->
    <style name="Theme.SteamTracker" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowBackground">@color/miuix_bg_dark</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="colorOnSurface">@color/miuix_text_primary_dark</item>
        <item name="colorOnSurfaceVariant">@color/miuix_text_secondary_dark</item>
    </style>

    <style name="Theme.SteamTracker.BangumiTransition" parent="Theme.SteamTracker">
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
